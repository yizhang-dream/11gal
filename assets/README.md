# 游戏资源目录

资源按类型分开，便于后续接入脚本或游戏引擎：

- `images/backgrounds/`：背景与空场景图
- `audio/`：音乐、音效、语音
- `characters/`：人物立绘、头像、差分

## 命名约定

- 目录使用小写英文和短横线，例如 `night-21`、`research-institute`。
- 图片文件保留团队已有的描述性中文名称，避免破坏现有引用。
- 新增图片优先使用 `地点_时间_视角.扩展名`，例如 `library_night-21_wide.jpg`。
- 同一地点的不同时间或版本放在对应的时间/版本子目录中。
